from django.db import models

# Create your models here.
# class sir_data(models.Model):
#    gamma = models.DecimalField(max_digits=5, decimal_places=2)
#    beta = models.DecimalField(max_digits=5, decimal_places=2)
#    _lambda = models.DecimalField(max_digits=5, decimal_places=2,
#              verbose_name='Control effectivness')
#    c = models.IntegerField(verbose_name='Control start')
#    kappa = models.DecimalField(max_digits=10, decimal_places=3)
#    pi = models.DecimalField(max_digits=5, decimal_places=3,
#         verbose_name='Peak intensity')